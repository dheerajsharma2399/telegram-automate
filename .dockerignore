# Git and version control
.git
.gitignore
.gitattributes

# Python cache and compiled files
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Testing and coverage
.tox/
.coverage
.pytest_cache/
htmlcov/
.coverage.*
coverage.xml
*.cover
.hypothesis/

# Documentation
docs/

# Keep README.md but exclude other markdown files during build
# (They can be included separately if needed)
README.md
*.md

# Except keep essential files
# !README.md - already included above

# Development and debugging
*.log
logs/
*.log.*
*.pid
*.seed
*.pid.lock
.nyc_output

# Temporary files
*.tmp
*.temp
tmp/
temp/

# Docker files (prevent recursive docker builds)
docker-compose*.yaml
docker-compose*.yml
.dockerignore

# Keep Dockerfile - it's needed for the build
# Note: Dockerfile can be explicitly copied in build commands

# Data files (these should be mounted as volumes)
data/
*.db
*.sqlite
*.sqlite3

# Configuration files (should be mounted or provided via environment)
.env.*
config.yaml
config.yml
config.json

# Test files
test_*.py
tests/
**/test_*.py

# Build artifacts
*.tar.gz
*.zip
*.rar

# Node modules (if any)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Certificate files (should be provided securely)
*.pem
*.key
*.crt
*.cer

# Backup files
*.bak
*.backup
*~

# Large files
*.mp4
*.mov
*.avi
*.pdf
!README.md